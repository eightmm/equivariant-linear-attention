{
  "schema_version": 1,
  "stage": "test_first_red",
  "commands": [
    {
      "command": "python3 -m pytest -q tests/test_benchmarking.py tests/test_edge_conditioned_local.py",
      "exit_code": 1,
      "classification": "environment_invocation_error",
      "outcome": "/usr/bin/python3 does not contain pytest; the locked project interpreter is .venv/bin/python"
    },
    {
      "command": ".venv/bin/python -m pytest -q tests/test_benchmarking.py tests/test_edge_conditioned_local.py",
      "exit_code": 2,
      "classification": "expected_test_first_failure",
      "outcome": "collection fails because _radius_candidate_edge_index is not implemented; EC transport and sparse batch fields are likewise absent"
    }
  ],
  "interpretation": "The new sparse-plumbing and edge-conditioned-local contracts fail before implementation, as required by the test-first gate."
}
